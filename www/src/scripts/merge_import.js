// Merge neutralized to prevent augmenting `window.testData` at runtime.
// This file is intentionally a no-op to ensure the embedded canonical dataset is authoritative.
(function(){
  if (typeof window === 'undefined') return;
  // no-op intentionally
})();
